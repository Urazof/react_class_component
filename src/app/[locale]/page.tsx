// Main search page — Phase 2 will implement full SSR with server components.
// Placeholder so Next.js boots and [locale] route resolves.
export default function HomePage() {
  return <div>Loading...</div>;
}
